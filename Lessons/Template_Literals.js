// Temperate Literals

const name = "Anna";
const country = "Canada";
const age = 33;

console.log("Name " + name + " Country " + country + " Age " + age);

//T.L allows you a more elegant solution to join values and strings

console.log(`Name ${name.length} Country ${country} Age ${age}`);

// ^ can use functions within back ticks too (.length)